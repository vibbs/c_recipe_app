{
  "name": "client_src",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.1",
    "encodeurl": "^1.0.2",
    "firebase": "^5.5.9",
    "jquery": "^3.5.0",
    "react": "^16.2.0",
    "react-chips": "^0.6.4",
    "react-dom": "^16.2.0",
    "react-firebaseui": "^3.1.2",
    "react-router": "^4.2.0",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.1.1",
    "react-select": "^2.0.0"
  },
  "scripts": {
    "start": "set PORT=3001 && react-scripts start",
    "build": "rm -rf ../client && mkdir ../client && react-scripts build && cp -r build/* ../client/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
